export const HeroSection = () => {
  return (
    <section className="w-full h-screen bg-white flex flex-col items-center justify-center transition-opacity duration-1000 animate-fade-in">
      {/* Content will be added here later */}
    </section>
  );
};
